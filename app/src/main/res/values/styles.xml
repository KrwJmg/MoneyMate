<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SpotifyDatePickerDialog" parent="Theme.MaterialComponents.Dialog">
        <item name="android:background">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorAccent">@color/mint</item> <!-- ใช้ mint -->
        <item name="android:buttonBarPositiveButtonStyle">@style/DialogButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogButton</item>
    </style>

    <style name="DialogButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/mint</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>